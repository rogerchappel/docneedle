# Release Checklist

Before release:

- Run the local test suite.
- Run the smoke command.
- Confirm the rollback note is current.
- Keep promotion copy separate from release artifacts.

Escalate if a package script fails or if the generated artifact includes private context.
