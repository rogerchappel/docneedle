# Release runbook

The release owner checks the changelog, runs the smoke suite, and opens a rollback note before publish.

## Escalation

If a fixture smoke fails, escalate to the maintainer with the exact command, environment, and changed files.

## Safety

No credential files are indexed. Keep outputs local unless a human explicitly shares them.
